import { Button } from "@mui/material";

export const TablePage = () => {
  return (
    <>
      <Button variant="contained" size="small" onClick={() => { }}>Hello</Button>
      <Button variant="contained" size="medium" onClick={() => { }}>Hello</Button>
      <Button variant="contained" size="large" onClick={() => { }}>Hello</Button>
      <Button variant="text" onClick={() => { }}>Hello</Button>
    </>
  )
}